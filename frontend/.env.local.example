# Backend API (local dev). On Render, set to your deployed backend URL.
NEXT_PUBLIC_API_URL=http://localhost:8000

# Supabase (optional for demo). Leave blank to run in demo mode without auth.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
